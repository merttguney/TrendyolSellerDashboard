{
  "name": "trendyol-fullstack",
  "version": "1.0.0",
  "description": "Trendyol entegrasyon projesi",
  "scripts": {
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && PORT=3001 npm start\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "test": "cd backend && npm test"
  },
  "keywords": [
    "trendyol",
    "e-commerce",
    "fullstack"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 